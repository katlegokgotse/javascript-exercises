let number = 10
let floatNumber = 10.0

const addition = (integer, float) => integer + float
const subtractions = (integer, float) => integer - float
const multiplication = (integer, float) => integer * float
const division = (integer, float) => integer / float
const modulous = (integer, float) => integer % float
const exponentiation = (integer, float) => integer ** float

console.log(addition(number, floatNumber))
console.log(subtractions(number, floatNumber))
console.log(multiplication(number, floatNumber))
console.log(division(number, floatNumber))
console.log(modulous(number, floatNumber))
console.log(exponentiation(number, floatNumber))